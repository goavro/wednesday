sudo: required

language: go

go:
  - 1.6
  - tip

services:
  - docker

install:
  - go get -u github.com/Masterminds/glide
  - GOOS=linux GOGC=off go build -v

script: go test -tag=\!integration -v $(glide novendor)
